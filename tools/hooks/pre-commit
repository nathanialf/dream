#!/bin/sh
# Gate: no ROM-derived content, and the round trip must still be byte-identical.
set -e
cd "$(git rev-parse --show-toplevel)"
tools/check_no_rom.sh
if [ -f baserom/DREAM.sfc ]; then make -s check; else echo "pre-commit: no baserom/DREAM.sfc, skipping build gate"; fi
